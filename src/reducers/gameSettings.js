// import { actions } from '../types'

const initialState = {
  isLive: false,
  playerTurn: false,
  strict: false,
  maxRounds: 20
}

function gameSettings(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}

export default gameSettings
